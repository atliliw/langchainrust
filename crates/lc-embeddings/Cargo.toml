[package]
name = "lc-embeddings"
version = "0.15.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Embedding model implementations for langchainrust — OpenAI, DeepSeek, Qwen, local/ONNX"
repository = "https://github.com/atliliw/langchainrust"
rust-version = "1.82"

[dependencies]
lc-core = { path = "../lc-core", version = "0.15.0" }
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
thiserror = "2.0"
futures-util = "0.3"
log = "0.4"
ort = { version = "2.0.0-rc.13", optional = true }
ndarray = { version = "0.17", optional = true }
fastembed = { version = "5", optional = true }
tokenizers = { version = "0.22", optional = true }

[features]
default = []
local-embeddings = ["ort", "ndarray", "dep:tokenizers"]
fastembed = ["dep:fastembed"]

# 🎉 LangChain Rust 中文使用指南

## ✅ 已完成的核心功能

### 1. RunnableConfig - 执行配置

**用途**: 配置 Runnable 的执行参数

**示例**:
```rust
use langchainrust::RunnableConfig;

// 创建配置
let config = RunnableConfig::new()
    .with_tag("生产环境")              // 添加标签
    .with_run_name("我的运行")         // 设置运行名称
    .with_max_concurrency(10);        // 设置最大并发数

// 合并配置
let config1 = RunnableConfig::new().with_tag("标签1");
let config2 = RunnableConfig::new().with_tag("标签2");
let merged = config1.merge(config2);  // 合并后的配置包含两个标签
```

---

### 2. Runnable Trait - 核心接口

**用途**: 定义所有组件的统一接口

**核心方法**:
- `invoke()` - 执行单个输入
- `batch()` - 批量处理多个输入
- `stream()` - 流式输出 (需要重写)

**示例**:
```rust
use langchainrust::core::runnables::Runnable;
use langchainrust::RunnableConfig;
use async_trait::async_trait;

// 定义一个简单的加法器
struct AddOne;

#[async_trait]
impl Runnable<i32, i32> for AddOne {
    type Error = std::convert::Infallible;

    async fn invoke(&self, input: i32, _config: Option<RunnableConfig>) -> Result<i32, Self::Error> {
        Ok(input + 1)
    }
}

// 使用
#[tokio::main]
async fn main() {
    let runnable = AddOne;
    
    // 单次执行
    let result = runnable.invoke(5, None).await.unwrap();
    println!("结果: {}", result);  // 输出: 6
    
    // 批量处理
    let results = runnable.batch(vec![1, 2, 3], None).await.unwrap();
    println!("批量结果: {:?}", results);  // 输出: [2, 3, 4]
}
```

---

### 3. Message Types - 消息类型

**用途**: 表示聊天对话中的各种消息

**支持的消息类型**:
- `System` - 系统消息
- `Human` - 用户消息
- `AI` - AI 助手消息
- `Tool` - 工具返回消息

**示例**:
```rust
use langchainrust::{Message, MessageType};
use serde_json::json;

// 创建系统消息
let system_msg = Message::system("你是一个有用的 AI 助手");

// 创建用户消息
let user_msg = Message::human("你好，请介绍一下 Rust 语言");

// 创建 AI 消息
let ai_msg = Message::ai("Rust 是一门系统编程语言...");

// 创建工具消息
let tool_msg = Message::tool("call_123", "计算结果: 42");

// 链式配置
let msg = Message::human("问题")
    .with_name("张三")                    // 设置发送者名称
    .with_id("msg_001")                   // 设置消息 ID
    .with_additional_kwarg("priority", json!("high"));  // 添加元数据

// 获取消息类型
println!("消息类型: {}", user_msg.type_str());  // 输出: human
```

---

## 📝 完整示例：构建一个简单的对话链

```rust
use langchainrust::{Message, RunnableConfig, Runnable};
use async_trait::async_trait;

// 定义一个简单的对话处理器
struct ConversationProcessor;

#[async_trait]
impl Runnable<Vec<Message>, String> for ConversationProcessor {
    type Error = std::convert::Infallible;

    async fn invoke(&self, messages: Vec<Message>, _config: Option<RunnableConfig>) -> Result<String, Self::Error> {
        // 简单示例：提取最后一条消息
        if let Some(last_msg) = messages.last() {
            Ok(format!("收到消息: {}", last_msg.content))
        } else {
            Ok("没有消息".to_string())
        }
    }
}

#[tokio::main]
async fn main() {
    // 创建对话
    let messages = vec![
        Message::system("你是一个助手"),
        Message::human("你好"),
        Message::ai("你好！有什么可以帮你的？"),
        Message::human("请介绍一下 Rust"),
    ];

    // 处理对话
    let processor = ConversationProcessor;
    let result = processor.invoke(messages, None).await.unwrap();
    
    println!("{}", result);
}
```

---

## 🧪 运行测试

```bash
# 运行所有测试
cargo test

# 运行特定测试文件
cargo test --test runnable_test
cargo test --test message_test

# 显示测试输出
cargo test -- --nocapture
```

---

## 📁 项目结构说明

```
src/
├── lib.rs                          # 库入口
├── core/
│   ├── mod.rs                      # 核心模块入口
│   └── runnables/
│       ├── mod.rs                  # Runnable 模块入口
│       ├── config.rs               # ✅ 执行配置
│       └── runnable_trait.rs       # ✅ Runnable trait
└── schema/
    ├── mod.rs                      # 数据模型入口
    └── messages/
        ├── mod.rs                  # 消息模块入口
        └── message.rs              # ✅ 消息类型

tests/                              # 测试文件
├── runnable_config_test.rs         # 配置测试
├── runnable_test.rs                # Runnable 测试
└── message_test.rs                 # 消息测试
```

---

## 🎯 下一步开发建议

### Phase 2: LLM 实现
1. **BaseLanguageModel trait** - LLM 基础接口
2. **OpenAI 实现** - HTTP 客户端 + SSE 流式响应
3. **工具调用** - Function Calling 支持

### Phase 3: Agents 系统
1. **Tool trait** - 工具接口 (带 JSON Schema)
2. **工具注册表** - 管理可用工具
3. **AgentExecutor** - Agent 执行器

### Phase 4: 高级功能
1. **Chains** - 链式调用
2. **Memory** - 对话记忆
3. **RAG** - 检索增强生成

---

## 💡 重要提示

1. **异步编程**: 所有方法都是 `async`，需要在 `tokio` 运行时中使用
2. **类型安全**: Rust 的强类型系统确保编译时错误检查
3. **错误处理**: 使用 `Result` 类型处理可能的错误
4. **配置传递**: `RunnableConfig` 可以在组件间传递和合并

---

## 📚 参考资源

- Python 源代码: `langchain/libs/core/langchain_core/`
- 实施计划: `IMPLEMENTATION_PLAN.md`
- 完成总结: `COMPLETION_SUMMARY.md`

---

## ✨ 总结

**已完成**: 
- ✅ RunnableConfig (配置管理)
- ✅ Runnable Trait (核心接口)
- ✅ Message Types (消息系统)

**测试覆盖**: 23个测试全部通过 ✅

**代码质量**: 
- 中文注释完整
- 文档示例清晰
- 编译无警告

**准备就绪**: 可以继续开发更高级的功能！🚀